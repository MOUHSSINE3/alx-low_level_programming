#!/bin/bash
#include<stdio.h>
/**
 * main - Entry a ligne with using printf function;
 * Return: 0 (success);
 */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art",\n);
	return (0);
}
